import IconBackgroundGeneration from "./IconBackgroundGeneration";

export default function IconBackgroundContainer() {
  return (
    <section>
      <IconBackgroundGeneration />
    </section>
  );
}
